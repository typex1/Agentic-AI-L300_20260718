# Python dependencies for this workspace.
# Pinned to the versions installed and verified in this environment.
#
# Install with:  pip install -r requirements.txt

# --- Core Strands Agents SDK ---
strands-agents==1.45.0
strands-agents-tools==0.8.2        # calculator, shell tools

# --- Web search tool (examples/1-*, examples/2-*) ---
ddgs==9.14.4

# --- AgentCore Runtime deployment (examples/2-*) ---
bedrock-agentcore==1.18.1
bedrock-agentcore-starter-toolkit==0.3.10
requests==2.34.2

# --- AWS SDK ---
boto3==1.43.39

# --- Structured output ---
pydantic==2.13.4

# --- NOT pip-installable (installed separately via 0-install.sh) ---
# * uv / uvx  -> curl -LsSf https://astral.sh/uv/install.sh | sh
#   Used by: MCP servers, strands-shell
